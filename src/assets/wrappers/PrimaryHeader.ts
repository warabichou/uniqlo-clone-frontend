import styled from 'styled-components';

const Wrapper = styled.div`
    header {
        h2 {
            padding: 1.5625rem 0;
            font-size: 1.5rem;
            font-weight: 300;
            line-height: 1.4;
            text-align: center;
        }
    }
`;

export default Wrapper;
