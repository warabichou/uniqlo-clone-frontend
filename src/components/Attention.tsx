import Wrapper from '../assets/wrappers/Attention';
import { PrimaryHeader } from '.';

const Attention = () => {
    return (
        <Wrapper>
            <PrimaryHeader title="注目の情報" />
        </Wrapper>
    );
};

export default Attention;
